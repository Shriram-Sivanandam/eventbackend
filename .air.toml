root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/app ./cmd/api"
  bin = "tmp/app"
  delay = 1000
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "node_modules"]

[log]
  time = true